# Sales Log

| Date | Channel | Action | Expected outcome | Follow-up |
|---|---|---|---|---|
| 2026-08-22 | (a) GitHub landing | Rewrote README as buyer-facing landing page: product catalog w/ per-call prices, x402 purchase-flow diagram, "run your own instance" quick start, honest proven-vs-in-progress status, target-audience section. Set repo description + 8 discovery topics (x402, ai-agents, micropayments, base, usdc, …) via `gh repo edit`; pushed to master (commit on 2e6c363+). | Organic visitors convert: clearer value prop for agent-framework builders; topics make repo discoverable in x402/micropayments searches; issue tracker positioned as contact path for hosted-endpoint waitlist. Watch repo stars/traffic + issues over next 7 days. | 2026-08-29 |
| 2026-08-22 | notifications check | Reviewed `gh api notifications`: all items are own CI runs (fund-and-settle, sui-settlement, agent-mine-test failures) — zero inbound responses to date. No hot leads. No prior outreach existed (first sales shift). | n/a | next shift |
| 2026-08-22 (shift 2) | (b) marketplace/directory + facilitator operator contact | Executed channel (b). Researched all listing surfaces live: x402-discovery-index, PayAPI Market, Agent402, CDP Bazaar — **all require a publicly reachable origin serving a spec-conformant 402 challenge; every one verifies before/during listing** (discovery-index DNS-checks submissions; PayAPI badge requires a real settlement from their buyer wallet; Agent402 crawls hourly and de-routes unreachable origins). Honesty gate: our Base-rail server (market_server.py) uses MockFacilitator = simulated settlement, so listing it would be fraud; Sui rail settles real on-chain but devnet SUI custom dialect. PROVEN instead: public HTTPS from this box works — localhost.run SSH tunnel live at https://a07dd1999841eb.lhr.life serving real public 402 challenges off sui_market_server :8604 (verified end-to-end via external curl). ONE outbound contact filed: integration inquiry to DrVelvetFog (sui-x402-facilitator — non-custodial exact-scheme-on-Sui verify/settle, mainnet+testnet USDC, zero fees): discovery surface? migration path off our custom dialect? testnet-USDC onramp? → https://github.com/DrVelvetFog/sui-x402-facilitator/issues/1. This rail is the shortest honest path to being listable everywhere without an EVM port. Full findings in org/research/findings.md (RQ-003 answered). | Operator reply within ~1 week → migration spec for builder; if facilitator offers/displays listed resources, we list day one after migrating to exact scheme. Tunnel is anonymous/ephemeral — ops must make it persistent (named tunnel or free keyed subdomain) before any public listing cites the URL. | 2026-08-29 (or on reply) |
| 2026-08-22 (shift 3) | (c) x402/Base builder outreach | Executed channel (c). Scanned live x402 ecosystem issues; shortlisted agentopoly (0xgleb) — an actively-built (pushed same day) P2P agent economy whose issue #17 "x402 interoperability is undefined" explicitly requires **real response fixtures from a real provider** before coding their advertised-x402-HTTP-service boundary. Filed ONE personalized comment (https://github.com/0xgleb/agentopoly/issues/17#issuecomment-5381317261) delivering immediate value: both rails' genuine 402 challenges captured live (Base-USDC exact-scheme-shaped + Sui custom dialect), with honest disclosures (Base rail = mock settlement, usable for challenge-side validation only; Sui dialect = useful interop edge case for their malformed-provider tests) + offer to spin up an ephemeral public tunnel on request during their test window. Ruled out rsi-eaf (XRPL rail mismatch), Haven-AI (internal coordination channel), ATM (fellow seller, not buyer). | If agentopoly adopts our fixtures as pinned test material, our service becomes the reference provider inside their agent-discovery surface — a warm inbound path when their x402 capability ships. Reply also signals engagement; tunnel request = strong buying signal. | 2026-08-29 |

## Notes
- Honesty guard: no public hosted endpoint exists yet — landing copy says so explicitly and routes interested parties to open a GitHub issue (waitlist signal). Never claim hosted availability until it's deployed.
- Rotation state: (a) shift 1, (b) shift 2. Next shifts: (c) x402/Base builder outreach or (d) demo/tutorial post. NOTE: further marketplace listings are BLOCKED until the endpoint is public + spec-conformant — see shift 2 entry; don't re-attempt channel (b) until facilitator migration lands.
